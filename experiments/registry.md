# Experiment Registry

Every experiment MUST be registered here BEFORE running. One row per experiment.

## Schema

| Field | Meaning |
|---|---|
| ID | `EXP-XXX` (zero-padded, monotonically increasing) |
| Phase | Which roadmap phase requested it |
| Hypothesis | H1 / H2 / H3 / H4 / exploratory / variance |
| Objective | One sentence — what question does it answer? |
| Configuration | Agents, seeds, N matches, deck version, git tag |
| Expected result | What outcome would confirm/reject the hypothesis? |
| Actual result | Filled in after the run — Wilson CI, p-value, verdict |

## Ledger

| ID | Phase | Hypothesis | Objective | Configuration | Expected | Actual |
|---|---|---|---|---|---|---|
| EXP-001 | 0 | baseline | Establish the Kaggle-ladder floor and validate the end-to-end submission pipeline (bundle → upload → Validation Episode → rating). | Agent: `RandomAgent` (uniform over `obs["select"]["option"]`, `maxCount` draws). Deck: `decks/selected/deck.csv` (Phase 0 placeholder — Kaggle sample deck). Git tag at submission: `v0.1-setup`. Seeds: RNG in `main.py` unseeded (Kaggle worker RNG). N matches: as awarded by the ladder scheduler. Hardware: Kaggle sandbox (2 vCPU, 12.2 GiB). | Rating stabilizes below the heuristic baseline that arrives in Phase 1 (EXP-002). Concrete Phase 0 target: `Validation Episode` passes; ladder rating settles in the µ₀ = 600 neighborhood within the first week — no strong prior on the exact number. | Validation Episode: **Complete** (2026-07-01, ~27 min after upload). Stabilized rating after ~4 days: **364.6** (drift of −235 from μ₀ = 600). Ladder floor every subsequent submission must beat. Pipeline verified end-to-end. Full trace in `docs/submission-log.md` row #1. |
| EXP-002a | 1 | baseline | Local head-to-head: does the deterministic HeuristicAgent (first-`maxCount` selector) beat RandomAgent by a Wilson-CI-separated margin under the benchmark protocol? | Agents: `HeuristicAgent` (A) vs `RandomAgent` (B). Deck: `decks/selected/deck.csv` (Phase 0 placeholder). N = 200 matches, K = 1 replication (Phase 1 quick-iteration budget per `docs/benchmark-protocol.md`). Match seeds 1..200; agent seed = match seed. Deterministic engine seeding via `configuration={"randomSeed": seed}`. Hardware: local WSL2 (see `docs/engineering.md`). Runner: `scripts/local_ladder.py --agent-a heuristic --agent-b random --matches 200 --seed-start 1`. | Heuristic win rate above 0.5 with Wilson 95% CI strictly above 0.5 (i.e., lower endpoint > 0.5). If the CI covers 0.5, the DoD for Issue #8 is not met and we iterate on the heuristic before submission. | **DoD met.** N = 200. Wins / Draws / Losses = **151 / 0 / 49**. Win rate $\hat p$ = **0.755**. Wilson 95% CI = **[0.691, 0.809]** — lower endpoint 19.1 pp above 0.5. Zero draws: the `cabt` engine resolves this deck/ruleset to a decisive outcome. Interpretation: the engine's option ordering is not random — index 0 is systematically stronger — so a deterministic first-`maxCount` selector already dominates uniform sampling by a wide margin. Log: `results/heuristic_vs_random.jsonl`. Run date: 2026-07-01. |
| EXP-002b | 1 | baseline | Kaggle ladder: does the heuristic reach a higher stabilized TrueSkill rating than the random submission (EXP-001)? | Agent: heuristic shim (`submissions/heuristic_main.py`). Deck: `decks/selected/deck.csv` (unchanged from EXP-001). Git tag at submission: `v0.2-baselines`. Bundle via `python scripts/submit.py --main submissions/heuristic_main.py --out submission-heuristic.tar.gz`. | Stabilized rating > 600.0 (EXP-001 μ₀) by a margin that exceeds the ladder's per-match noise. No pre-registered threshold — this is directional. | Stabilized rating after ~4 days: **527.9** (+163.3 above EXP-001's 364.6). Beats random with a large margin, confirming the local head-to-head result (EXP-002a: 75.5% win rate, Wilson 95% CI [0.691, 0.809]). Both submissions remain below μ₀ = 600, indicating the pool median sits below the initial estimate — expected given the random+heuristic tier is at the bottom of the ladder. Full trace in `docs/submission-log.md` row #2. |
| EXP-003 | 3 | **H1** | Does SO-ISMCTS (random rollouts, uniform consistent determinization) beat the deterministic heuristic baseline head-to-head? First test of a pre-registered hypothesis. | Agents: `ISMCTSAgent` (A: iterations = 1000, c = √2, uniform consistent determinization, mirror-deck knowledge, random rollouts) vs `HeuristicAgent` (B). Deck: `decks/selected/deck.csv` (Phase 0 placeholder, mirror). N = 500 matches, paired seeds 1..500 (match seed = agent seed = env `randomSeed`). K = 1 replication — deviation from the protocol's K = 20, justified by wall-clock (~1 min/match ⇒ ~8 h for N = 500; K = 20 would be infeasible before the deadline; seed-stability is separately covered by the Phase 5 variance experiment). Hardware: local WSL2. Runner: `python scripts/local_ladder.py --agent-a ismcts --agent-b heuristic --matches 500 --seed-start 1 --iterations 1000 --out results/exp003_ismcts_vs_heuristic.jsonl`. Git tag at run: `v0.3-hypotheses`. **Validity flag:** total ISMCTS fallback events (unsearchable decisions) reported by the runner; a nonzero count outside setup turns triggers investigation before the verdict is recorded. | **H1 supported** iff ISMCTS win rate has Wilson 95% lower bound > 0.5. Prior from the readings: genuinely uncertain — Cowling's Dou Di Zhu result shows ISMCTS ≈ PIMC under extreme branching, and the heuristic's exploitation of the engine's option ordering (EXP-002a: 75.5% vs random) is a strong baseline. A null result is a publishable finding per the risk register. | **SUPPORTED (2026-07-09).** N = 500, **390W–0D–110L**, win rate **0.780**, Wilson 95% CI **[0.742, 0.814]** — lower bound 0.742, ~24 pp clear of the 0.5 threshold. **Validity: 0 fallbacks** (ISMCTS searched every decision; determinizer fixes in `notes/phase3-implementation-plan.md`). Run split across 2 sessions after two reboots (part1 seeds 1–342, part2 343–500) then merged; per-match JSONL flush made this lossless. Total wall-clock ~6.3 h, median 31.4 s/match. Data: `results/exp003_final.jsonl` (gitignored). **Interpretation caveat:** this is a *local head-to-head*, not the ladder; the decomposition of *why* ISMCTS wins (search value vs info-set-tree value vs hidden-info ceiling) is the job of the PIMC + oracle diagnostic ladder (`open-ideas.md`). Zero draws is consistent with EXP-002a — the engine resolves this deck to decisive outcomes. |
| EXP-004 | 3 | diagnostic | Diagnostic-ladder arm: does PIMC (Determinized UCT — K independent trees + visit vote) beat the heuristic, and by how much vs ISMCTS? The gap `W_ISMCTS − W_PIMC` isolates the value of the shared information-set tree (Cowling's contribution) in PTCG. | Agents: `PIMCAgent` (A: iterations = 1000 total, K = 10 trees × 100 iters each, c = √2, uniform consistent determinization, mirror-deck) vs `HeuristicAgent` (B). Deck: `decks/selected/deck.csv` (mirror). N = 500 paired seeds 1..500 — **same seeds as EXP-003** so the comparison is paired at the match level. Runner: `python scripts/local_ladder.py --agent-a pimc --agent-b heuristic --matches 500 --seed-start 1 --iterations 1000 --out results/exp004_pimc_vs_heuristic.jsonl`. Same validity flag (fallbacks). Not gating; interpretive. | PIMC win rate with Wilson 95% CI. Then `W_ISMCTS − W_PIMC` with a paired test (McNemar / paired bootstrap on the shared 500 seeds). Prior: PIMC should also beat the heuristic (search value is large per EXP-003); whether ISMCTS adds a further margin is the open question — Long's framework predicts a small gap if PTCG has high leaf correlation / fast disambiguation. | **DONE (2026-07-10).** N = 500, **371W–0D–129L**, win rate **0.742**, Wilson 95% CI **[0.702, 0.778]**. Validity: 0 fallbacks. ~6.8 h, mediana 32.7 s/match. **Paired vs EXP-003 (same seeds):** both win 292, both lose 31, ISMCTS-only 98, PIMC-only 79 → raw Δ = **+3.8 pp** for ISMCTS; **McNemar exact p = 0.176 (n.s. at 5%)**. Interpretation: the value in PTCG is overwhelmingly *search itself* (heuristic → PIMC: +24 pp); the shared info-set tree adds a further +3.8 pp that this sample cannot distinguish from noise. This is the "Dou Di Zhu-adjacent" regime and **confirms the Long-framework prediction chain** registered in `phase2-long-2010-notes.md` §5.1 (moderately high leaf correlation / disambiguation ⇒ small ISMCTS-over-PIMC headroom). Detecting a true 3.8 pp gap at 80% power would need roughly N ≈ 1,500–2,000 — a candidate Phase-5 extension, not a re-run now. Data: `results/exp004_pimc_vs_heuristic.jsonl` (gitignored). |
| EXP-005 | 3 | diagnostic | Diagnostic-ladder ceiling: cheating UCT (true hidden state, no determinization) vs heuristic. `W_cheat − W_ISMCTS = Δ_ceiling`, the cost of imperfect information and the headroom for belief-based ideas (`open-ideas.md`). | **Feasible (probe 2026-07-09).** `visualize_data()` exposes the FULL ground truth for both players — `deck` as an ordered list of `{id, serial, playerIndex, name}`, plus prizes and hands — even though the normal observation hides all of it. So a true oracle is buildable: parse the current step of `visualize_data`, extract each player's real deck order / prizes / hand, and feed that exact hidden state into `search_begin` (which already accepts a caller-supplied determinization). Remaining work: a small parser for the `visualize_data` payload (a list keyed by game step; take the current one) + a `decide_oracle` that skips sampling. NOT a submission (cheating violates the rules; local diagnostic only). | `W_cheat − W_ISMCTS` with Wilson CIs and a paired test on the shared 500 seeds; the sign and size gate the Phase-5 belief pipeline. | **DONE (2026-07-10).** N = 500, **414W–0D–86L**, win rate **0.828**, Wilson 95% CI **[0.793, 0.859]**. Validity: 0 fallbacks. ~6.2 h. **Δ_ceiling pareado (mesmos seeds do EXP-003): +4.8 pp** (93 só-oracle vs 69 só-ISMCTS; McNemar exato p = 0.070, **n.s. a 5%**). **Predição registrada (Long §4.2, "médio 5–15 pp"): errou por pouco** — o real (4.8) ficou logo abaixo da banda; direção certa, magnitude superestimada. **Decisão de gate (regra pré-registrada em `open-ideas.md`): Δ ≲ 5 pp ⇒ a linha de belief-refinement *além da consistência* está morta para o contexto espelho** — nem um modelo de crença perfeito compraria mais que ~5 pp aqui. Distinção importante que o gate NÃO cobre: na ladder o déficit dominante não é falta de modelo de crença sofisticado, é falta da *lista* do oponente (determinização-filler ≪ consistente). Inferir o deck/arquétipo do oponente na ladder visa *recuperar a consistência* — pergunta diferente da que EXP-005 responde, e permanece aberta para a Fase 5. Data: `results/exp005_oracle_vs_heuristic.jsonl` (gitignored). |
| EXP-006 | 4 | **H2** | Do heuristic-guided rollouts beat random rollouts within ISMCTS? Second pre-registered hypothesis. | Agents: `ISMCTSAgent` + guided rollout (`ismcts-guided`: MoveScorer F1–F6, uniform weights 1.0, ε-greedy ε = 0.2 fixed, multi-pick selects fall back to random) vs `HeuristicAgent` reference — compared PAIRED against EXP-003 (`ismcts` random-rollout arm) on the same 500 seeds 1..500, 1000 iterations, mirror deck. Runner: `python scripts/local_ladder.py --agent-a ismcts-guided --agent-b heuristic --matches 500 --seed-start 1 --iterations 1000 --out results/exp006_guided_vs_heuristic.jsonl`. **Preconditions:** (1) option-type constants confirmed by `scripts/probe_option_types.py` (scorer misfires silently otherwise); (2) 10-match pilot with 0 fallbacks and sane per-match timing. Validity flag: fallbacks ≈ 0. | **H2 supported** iff the paired McNemar test vs EXP-003 favors guided at p < 0.05. Honest prior after the diagnostic ladder: uncertain, leaning small — search already extracts most available value (oracle gap only +4.8 pp), and Browne's heavy-rollout failure modes (bias, slowdown, over-determinism) are real. A null H2 is coherent with the "search dominates" story and publishable. | **NOT SUPPORTED (2026-07-11).** N = 500, **405W–0D–95L**, win rate **0.810**, Wilson 95% CI **[0.773, 0.842]**. Validity: 0 fallbacks. ~5.3 h, median 25.6 s/match (vs 31.4 s in EXP-003 — guided rollouts terminate games sooner). **Paired vs EXP-003 (same 500 seeds):** both win 314, guided-only 91, random-only 76, neither 19 → Δ = **+3.0 pp** for guided, **McNemar exact p = 0.279 (n.s. at 5%)** — fails the pre-registered p < 0.05 criterion. Interpretation: coherent with the diagnostic-ladder story (search dominates; EXP-003→005). The plain-ISMCTS-to-oracle headroom was only +4.8 pp, so a rollout-policy change had little room to show a detectable effect at N = 500; descriptively, guided (0.810) sits 1.8 pp below the oracle ceiling (0.828). The speed side-effect is real and feeds H3/time-budget work: at a fixed *time* budget (Kaggle's regime, unlike this fixed-iteration test) faster rollouts buy more iterations. Data: `results/exp006_guided_vs_heuristic.jsonl` (gitignored). |
| EXP-007 | 4 | exploratory | Deck selection (ADR-002): which of 4 candidate lists is the most robust under our own agent? Measures DECKS, not agents — both seats run the identical agent. | Round-robin over `decks/candidates/{current-v1, v1-tuned, aggro-fire, emboar-evolution}.csv`. Both seats: the best ISMCTS variant per the EXP-006 verdict (`ismcts-guided` if H2 holds, else `ismcts`) — **decided 2026-07-11, before any EXP-007 match: `ismcts`** (H2 not supported; the pre-registered rule applies as written). Per unordered pair: 2 runs (both seat orderings) × N = 50 paired seeds 1..50, 1000 iterations ⇒ 100 matches/matchup, 600 total. Wall-clock: originally estimated ~5 h from mirror timings; asymmetric matchups run ~6× longer (median 106–258 s/match — longer games), so the honest estimate is **~25–35 h**, run resumably across sessions. Incident 2026-07-11, before any cell was read: "up to N" selects crashed the agent when a determinization offered fewer options than `maxCount` (seed 37, current-v1 vs aggro-fire); fixed with clamps at every choice site, protocol unchanged, and the 86 already-recorded matches remain valid (identical trajectories under the fix — any match that had hit the condition would have crashed, none did). Runner: `bash scripts/run_exp007.sh <agent>` (wraps `local_ladder.py --deck-a/--deck-b`). Validity flag: fallbacks must be 0 in every cell — a nonzero count means the determinizer mishandles a card in that list; investigate before reading the cell. | Selection rule (pre-registered): pick the candidate with the best WORST-CASE matchup win rate (maximin over its row, both orderings pooled per matchup, Wilson 95% CI). Tie-break: fallbacks, then median match time. A candidate replaces v1 only if its pooled rate vs `current-v1` has Wilson lower bound > 0.5. Multiple comparisons: ADR-002 reports raw and Bonferroni-corrected intervals for the cells it cites. | **DONE (2026-07-15). Selected: `current-v1`.** 600 matches (12 ordered pairs × 50 paired seeds), **0 fallbacks, 0 draws** (validity flag clean). **Maximin (worst-case matchup win rate, both orderings pooled to 100 games/cell):** `current-v1` **0.66** (vs v1-tuned) ≫ v1-tuned 0.34 · aggro-fire 0.09 · emboar-evolution 0.13. `current-v1` is the maximin winner by a wide margin and dominates all three rivals head-to-head (66/100 vs v1-tuned, 91/100 vs aggro-fire, 87/100 vs emboar). **Robustness to timeout handling:** excluding the timeout rows (see below) the ranking is unchanged — 0.66 / 0.34 / 0.08 / 0.11. **Replacement gate NOT triggered:** no candidate beats `current-v1` (best challenger v1-tuned only 34/100 → Wilson well below 0.5), so `current-v1` stays. **Cited cells (current-v1 row), raw Wilson / Bonferroni m=3 (z=2.394):** vs v1-tuned [0.563, 0.745] / [0.541, 0.762]; vs aggro-fire [0.838, 0.952] / [0.818, 0.958]; vs emboar [0.790, 0.922] / [0.769, 0.931] — all lower bounds clear 0.5 even Bonferroni-corrected. **Timeout finding (not a bug):** 61 rows across the Fire×Fire and Fire×v1-tuned cells hit the engine's per-match TIMEOUT (status one side DONE +1, other TIMEOUT → forfeit; competitively valid). Concentrated by deck: timeout% / median-s = current-v1 **0.7% / 196s**, v1-tuned 11.0% / 317s, aggro-fire 24.3% / 530s, emboar-evolution 20.7% / 539s. The Fire decks grind long games that blow the ISMCTS time budget at 1000 iters — a *second* argument for `current-v1` under Kaggle's fixed-**time** (not fixed-iteration) regime, and a signal feeding #27/H3. Decision recorded in `docs/adr/adr-002-why-this-deck.md`. Data: `results/exp007_*.jsonl` (gitignored). |
| EXP-008 | 4 | exploratory (feeds H3) | Time-budget calibration (#27): map ISMCTS wall-clock cost vs iterations on the selected deck and choose the submission-time budget policy — fixed iterations vs per-move anytime cap — that maximizes strength with **zero forfeits** under Kaggle's ~600 s/agent/match budget. Motivated by EXP-007: at 1000 fixed iters even `current-v1` has p90 match time 617 s (> the ~600 s bank) and the grindy fields forfeit 11–24 %. | Our seat: submission agent (`ismcts`, `current-v1`). **Opponent field:** `{current-v1, v1-tuned, aggro-fire, emboar-evolution}` — captures matchup-dependent game length; the Fire grinds are the binding tail. **Cost model (pre-registered decomposition):** per-agent cumulative time $T(\text{it}) \approx c(\text{it})\cdot M$, where $c(\text{it})=\alpha+\beta\cdot\text{it}$ is the mean per-decision cost (linear in iterations) and $M$ = decisions/agent/game (iteration-independent — game length depends on decisions, not on how long each takes). Measure $c$ cheaply (per-decision timings over a few games/matchup at iters ∈ {300, 600, 1000, 1500}) and the $M$ distribution from many fast low-iter games, then **predict** the cumulative tail without running the slow high-iter full games; confirm the chosen operating point with one verification run. **Instrumentation (precondition, code written + reviewed, tests NOT run before this registration is acted on):** (1) runner logs per-decision time and per-agent cumulative; (2) runner logs decisions/agent/game; (3) optional per-move anytime cap in `decide()` (stop when iterations **or** `t_move` seconds elapse — ISMCTS is anytime, root = max visits so far). **Precondition — CONFIRMED 2026-07-15** from `make("cabt")`: `actTimeout = 0` (**no per-decision cap** — that constraint is dropped as vacuous), `runTimeout = 2000` (secondary whole-episode backstop), per-agent **`remainingOverageTime = 600`** in the agent's own observation (`shared: false`), spec: *"disqualified with TIMEOUT when this drops below 0"*. With `actTimeout = 0` all think time draws from the 600 s bank — the single binding constraint, matching EXP-007. **The bank is observable at decision time**, enabling an adaptive policy. Runner: `scripts/exp_timing.py` (new; extends the `smoke_ismcts.py` per-decision timer to many recorded games). Composes with #26/H3 (`exp_sensitivity_simulations.py`) which supplies strength(iters). | **Decision rule (pre-registered).** Single hard constraint: per-agent cumulative **p99 < 540 s** (600 − 10 % margin) with **0 forfeits** in a confirmation run. Policies: **A** fixed iters; **B** per-move anytime cap $t_{\text{move}}$ (safe by construction but still needs $p99[M]$); **C** adaptive — read live $R=$`remainingOverageTime`, allocate $t_{\text{move}}=\max(t_{\text{floor}}, (R-R_{\text{reserve}})/\hat m)$, which **cannot deplete the bank** and needs no $M$ prediction (robust to the unknown ladder field). Among policies meeting the constraint, pick the highest strength at the operating point (win rate vs the fixed heuristic reference, from H3); **prefer C, then B, over A when strengths overlap in Wilson 95 % CI**. Report fitted $c(\text{it})$ with $R^2$, the $M$ distribution (median / p95 / p99), derived safe $\text{it}^\*$ / $t_{\text{move}}^\*$ / adaptive params, and the confirmation-run forfeit count (must be 0). | **DONE (2026-07-16). Policy C selected; both gates PASS. Submission budget locked: `adaptive_budget=True`, `overage_reserve=60`, `budget_moves_ahead=80`.** **Fit stage (2026-07-15, 76 games).** Cost model $c(\text{it})=101\text{ms}+4.85\text{ms}\cdot\text{it}$ ($R^2=0.536$; per-decision median 1.56 / 2.80 / 4.32 / 7.93 s at 300 / 600 / 1000 / 1500 iters). $M$ (decisions/agent/game): median 23, observed max 69 (Fire decks; mirror only 40). **Fixed-iters rejected:** the point-estimate $c_\text{mean}\cdot p99[M]$ says it*≈1591 is "safe", but that underestimates the joint (high-$M$ ∧ costly-decision) tail — EXP-007 forfeited at 1000 fixed iters, so the fixed prediction is optimistic and fragile. **Operating point `overage_reserve=60`, `budget_moves_ahead=80` pre-registered here BEFORE the confirm run** (80 > observed max $M$ 69 + ladder margin; full-bank per-move $(600-60)/80 = 6.75$ s ≈ 1370 iters, above the EXP-003 1000-iter baseline, decaying gracefully; cannot deplete the bank by construction). **Confirm stage (2026-07-16): 80 games** (4 opponents × 20 paired seeds, iters-cap 100 000, opponent `ismcts` @ 1000 fixed iters), **1454 decisions**. **GATE 1 — forfeits 0/80: PASS.** **GATE 2 — cumulative p99 = 310.7 s < 540 s: PASS** (at n = 80 the nearest-rank p99 *is* the max, so 310.7 s is the worst game, 229.3 s of headroom; bank floor 292.2 s — never within 230 s of the 60 s reserve). Per opponent (cum p99 / $M$ max / win rate): aggro-fire 310.7 s / 68 / 0.85 · v1-tuned 283.1 s / 59 / 0.70 · emboar 188.0 s / 34 / 0.90 · mirror 165.1 s / 29 / 0.75. **Pre-registered choice validated ex post:** pooled max $M$ = 68 < `budget_moves_ahead` = 80, and per-decision median 5.95–6.26 s sits just under the 6.75 s full-bank cap. **Bank model confirmed exactly:** engine drawdown minus measured think-time has median and worst residual **+0.00 s over all 80 games** — every second of thinking is charged to the bank and nothing else is (`my_final_overage` is read at the *start* of the final decision, so the expected drawdown is cumulative − $t_\text{last}$). **Descriptive strength signal (NOT a verdict):** in the mirror cell both seats play `current-v1`, so the only difference is Policy C (≈1370 iters, decaying) vs fixed 1000 iters — Policy C wins 0.75, Wilson 95 % CI [0.53, 0.89], lower bound above 0.5. Suggests the adaptive budget buys *strength*, not merely safety; but n = 20, not pre-registered, and seat order is not balanced (first-player advantage is an uncontrolled confound) → #26/H3 is where this gets tested properly. **Conservatism quantified:** worst game uses 58 % of the 540 s target, the median game ~19 % — because `budget_moves_ahead` is a *constant* 80, the divisor never shrinks as the game progresses, so even a 68-move game finishes with 292 s unspent. That unspent bank is the price of not predicting $M$ — precisely the trade Policy C was chosen for (see the note's Lessons Learned). Bug fixed en route: the timing wrapper was a callable class, which `env.run`'s `getfullargspec` rejects → the first collection was all `status=ERROR` (0 decisions); replaced with a closure (regression test `tests/test_exp_timing.py`). Analyzers: `scripts/analyze_exp008.py` (fit), `scripts/analyze_exp008_confirm.py` (confirm gates + integrity guards). Data: `results/exp008_fit_*.jsonl`, `results/exp008_confirmC_*.jsonl` (gitignored). |
| EXP-009 | 4 | diagnostic (construct validity of EXP-003 / H1) | **What does not knowing the opponent's deck cost us?** Re-run EXP-003 changing exactly one thing — determinization — and price the gap. Motivated by the ladder: measured simultaneously on 2026-07-16, our heuristic sits at 497.8 and our ISMCTS at 475.4, i.e. ISMCTS **loses** to the baseline it beats 78 % locally. The registered suspect (EXP-005, `open-ideas.md`) is that the ladder hides the opponent's list, so hidden zones are filled with `FILLER_CARD` and the search plans deep inside a counterfactual board. This experiment turns a 4-day-latency, drift-confounded ladder signal into a one-night paired local measurement. | Agents: **`ismcts-filler`** (A — `opponent_deck_list=None`, `filler_card=1072`, i.e. the exact condition `submissions/ismcts_main.py` runs under) vs `heuristic` (B). Deck: `decks/selected/deck.csv` (`current-v1`, mirror). N = 500, **paired seeds 1..500 — the same seeds as EXP-003/004/005/006**, so McNemar runs on shared matches and the pairing is free. iterations = 1000, c = √2, uniform-random rollouts. **The ONLY difference from EXP-003 is the determinization condition**; everything else is held fixed, and `tests/test_local_ladder.py` asserts that mechanically (an arm that leaked `opp_deck` would silently rerun EXP-003, report "no gap", and invert the conclusion). Runner: `python scripts/local_ladder.py --agent-a ismcts-filler --agent-b heuristic --matches 500 --seed-start 1 --iterations 1000 --out results/exp009_ismcts_filler_vs_heuristic.jsonl --append`. Wall-clock ≈ 6 h (EXP-003 took 6.3 h at these settings), resumable per-match. Validity flag: fallbacks ≈ 0. **Sample size:** the predicted effect is large (~28 pp); EXP-004's registered power note puts N ≈ 1500–2000 at 80 % power for a *3.8 pp* effect, so N = 500 is comfortably powered for anything of the predicted size. Stated in advance: **if the true effect turns out small (< 5 pp), N = 500 is underpowered and the verdict is "inconclusive", not "no effect".** | Reference: EXP-003 (informed determinization) = **0.780**, Wilson [0.742, 0.814]. Prediction registered before the run: **filler drops substantially toward 0.5.** Branches, each with its consequence pre-committed: **(a) drops to ≈0.5 or below** ⇒ the ladder gap reproduces locally, determinization quality is the binding constraint, #29 must **not** ship "more search", and informed determinization is promoted from a Phase-5 idea to a Phase-4 candidate; **(b) drops but stays > 0.5** ⇒ determinization explains part of the gap and the cross-deck field explains the rest; **(c) no drop (~0.78)** ⇒ determinization is **not** the culprit, we are looking in the wrong place, and the ladder gap must be the cross-matchup field or something not yet named — a valuable negative that would save us from rebuilding the determinizer for nothing. **Known limitation, stated up front:** this is a **mirror** (both seats play `current-v1`), while the ladder differs from EXP-003 in *two* ways — filler determinization **and** an unknown cross-deck field. EXP-009 isolates the first only, so it can price that factor but cannot attribute the whole ladder gap. Report: win rate + Wilson 95 % CI, and paired McNemar vs EXP-003 on the shared 500 seeds. | **BRANCH (a) — DONE (2026-07-17).** N = 500 (seeds 1..500, no gaps/duplicates across the two interrupted sessions), **253W–0D–247L**, win rate **0.506**, Wilson 95 % CI **[0.462, 0.550]** — the CI covers 0.5: under the deployment determinization, 1000 iterations of ISMCTS are statistically indistinguishable from the zero-search heuristic. Validity: **0 fallbacks, 0 env errors**. **Paired vs EXP-003 (same 500 seeds):** both win 189, informed-only **201**, filler-only 64, neither 46 → Δ = **−27.4 pp**, McNemar exact **p = 1.2e-17** (265 discordant pairs). The registered prediction (~28 pp drop) was almost exact. **Consequences, as pre-committed:** the ladder gap reproduces locally; determinization quality — not search quantity — is the binding constraint; #29 must not ship "more search"; informed determinization is promoted to Phase 4 (→ EXP-010, registered 2026-07-17). An interim peek at n = 113 (filler 0.47, McNemar p ≈ 1.5e-07) was read and logged in the decision journal before completion; the decision rule did not move after the peek. **Timing note:** median 68.0 s/match vs 31.4 s in EXP-003 (total 11.2 h) — filler games run ~2× longer, consistent with the mechanism: a search that plans against an inert opponent picks slow, greedy lines, and the games drag. Data: `results/exp009_ismcts_filler_vs_heuristic.jsonl` (gitignored). |
| EXP-010 | 4 | exploratory (Issue #26 — determinization condition; decides what #29 ships) | **Which deployable determinization ships in the Simulation submission?** EXP-009's interim signal (read at n = 113, decision rule unchanged after the peek) is branch (a): filler determinization erases the search advantage (0.47 vs 0.81 informed, paired Δ ≈ −35 pp, McNemar p ≈ 1.5e-07). The registered follow-up applies: informed determinization is promoted to a Phase-4 candidate. The cheapest deployable improvement needs no inference at all: **assume the opponent plays our own list** (`ismcts-selfdeck`, `opponent_list_is_assumed=True` — clamped opponent-side accounting so revealed foreign cards don't fault). Hypothesis: a wrong-but-coherent opponent model (has energy, trainers, attackers — the simulated opponent fights back) ≫ an impossible-and-inert one (60× Snorlax). This experiment also fixes the *measuring instrument* before EXP-011 re-evaluates the deck: EXP-009 shows determinization moves the agent ~30 pp, ~2× the largest deck effect EXP-007 saw, so deck ranking under the wrong determinization would measure the delusion, not the decks. | Our seat: `current-v1`, three arms — **`ismcts-filler`** (deployed today), **`ismcts-selfdeck`** (candidate; deployable), **`ismcts`** (informed; **diagnostic ceiling only, NEVER submitted** — on the ladder the true list is unavailable). Opponent: `heuristic` piloting each of the 4 official starter decks `{iono, dragapult-ex, mega-abomasnow-ex-official, mega-lucario-ex}` — the only lists we hold that the field actually plays (mined archetype lists are gated on the Competition Rules check, `open-ideas.md` replay-deck-mining). Per cell: N = 50 paired seeds 1..50; 3 arms × 4 decks = 12 cells, 600 matches; iterations = 1000, c = √2, random rollouts. **Trajectory recording ON — first corpus run** (`--log-trajectories results/exp010_traj_<arm>_vs_<deck>.jsonl.gz`, per open-ideas trajectory-corpus; acceptable here because no timing quantity is being estimated). Runner: `bash scripts/run_exp010.sh` (per-cell resume). Runs **after EXP-009 completes** (same CPU). Wall-clock guess ~8–12 h (heuristic opponent is fast; asymmetric games run longer than mirrors). **Validity flags:** fallbacks must be 0 in the filler and informed arms; the selfdeck arm *can* legitimately fall back (assumed-pool shortage or no Basic among assumed hidden cards) — report its fallback count per cell and investigate before the verdict if > 1 % of decisions. | **Ship gate (pre-registered):** `ismcts-selfdeck` replaces the filler in `submissions/ismcts_main.py` iff it beats `ismcts-filler` on the pooled paired contrast (same seeds within each deck, McNemar exact, p < 0.05) AND is not worse in any single deck cell by Wilson-separated margin. Report per-deck win rates with Wilson 95 % CI (raw + Bonferroni m = 4) and the pooled contrast. **Ceiling:** `ismcts` − `ismcts-selfdeck` prices what archetype *inference* could still buy — that number decides how much Phase-5 effort informed-determinization deserves. **Branches:** (a) selfdeck ≫ filler ⇒ ship selfdeck in #29; (b) selfdeck ≈ filler ⇒ the coherence hypothesis is wrong, keep filler, inference (replay-deck-mining) becomes the only path; (c) selfdeck < filler ⇒ surprising — a coherent-but-wrong model is worse than an inert one (the search exploits a specific wrong deck harder than a null one); keep filler and record the mechanism question. Prior: (a), moderately confident — EXP-008's mirror cell showed the informed condition healthy, and selfdeck *is* informed in the mirror; the question is how it degrades off-mirror. | **BRANCH (a) — DONE (2026-07-19).** All 12 cells complete (N = 50 paired seeds each, 600 matches; analysis `scripts/exp010_analysis.py`). Pooled Wilson 95 %: filler **0.685** [0.618, 0.745] → selfdeck **0.795** [0.734, 0.845] → informed **0.845** [0.788, 0.889]. **Ship gate: PASS** — pooled paired selfdeck−filler **+11.0 pp**, McNemar exact **p = 0.023** (discordant 86: only-selfdeck 54, only-filler 32), and selfdeck is worse-by-Wilson-separation in **zero** deck cells (nominally better in all four: iono 0.78 vs 0.70, dragapult 0.92 vs 0.84, abomasnow 0.72 vs 0.58, lucario 0.76 vs 0.62; Bonferroni m = 4 CIs reported by the script). **Ceiling: informed − selfdeck = +5.0 pp, McNemar p = 0.24, n.s.** (58 discordant) — archetype *inference* has ≲ 5 pp of headroom on this field, echoing EXP-005's mirror ceiling (+4.8 pp); Phase-5 informed-determinization / replay-deck-mining effort is priced accordingly (low). Reference gap: informed − filler = +16.0 pp, p = 1.7e-4. A wrong-but-coherent opponent model recovers ~11 of the 16 pp the filler leaves on the table — the coherence hypothesis holds. **Validity:** fallbacks = 0 in all 12 cells, including selfdeck (the assumed-pool clamp never faulted, well under the 1 % flag). **Addendum (2026-07-19, surfaced by EXP-011's false start):** the informed arm has **1 env-error row** the first analysis did not check — seed 15 vs iono, our seat TIMEOUT at the engine's 600 s overage bank, scored as a loss. Both deployable arms (filler, selfdeck) closed with 0. Effect: the diagnostic ceiling is conservative by ≤ 0.5 pp (informed vs iono 41W counts the artifact as a loss); no gate quantity involves the informed arm, so the verdict is untouched. Data left as-is; the mechanism and the `--overage-bank` fix are recorded in EXP-011's amendment. **False alarm, caught and corrected same-day:** a first analysis read "0 losses / 8–21 draws per cell" across the 600 games. The producer's encoding is `outcome_for_a ∈ {1, 0, −1}` (`local_ladder._sign`: win/draw/loss); the analysis script guessed the encoding and misfiled the −1 losses as draws. Corrected tally: **0 draws in all 12 cells** (e.g. filler 35W–15L vs iono, 29W–21L vs abomasnow). No gate number moved — wins were counted correctly throughout — only the W/D/L labels were wrong. First payoff of the trajectory corpus: line 1 (`reward_a = −1` on a "no-loss" cell) is what contradicted the claim. Timing echoes EXP-009: vs abomasnow, median 105 s (filler) → 39 s (selfdeck) — a coherent simulated opponent shortens games. **Consequence, as pre-committed: `ismcts-selfdeck` replaces the filler condition in `submissions/ismcts_main.py` (#29).** Data: `results/exp010_*.jsonl` + first trajectory corpus `exp010_traj_*.jsonl.gz` (gitignored). EXP-011 unblocks with the instrument fixed. |
| EXP-011 | 4 | decision (ADR-002 revision gate — this is the re-evaluation the erratum promised as "EXP-010" before the renumbering) | **Does `current-v1` survive contact with the real field, measured with the fixed instrument?** EXP-007 selected it by maximin over a homemade 4-candidate pool, and ADR-002's erratum records the construct-validity flag: the pool was our own guesses, not the field. Two things changed since. We now hold the four official starter lists the ladder actually plays, and EXP-010 fixed the measuring instrument — under filler determinization a deck ranking would measure the delusion, not the decks (the determinization condition moves the agent 11–16 pp, the same order as the largest deck effect EXP-007 saw). | Our seat: **`ismcts-selfdeck`** — the exact shipping condition (1000 iterations, c = √2, uniform-random rollouts, assumed own-list determinization) — piloting each homemade candidate {`current-v1`, `v1-tuned`, `aggro-fire`, `emboar-evolution`}. Opponent: `heuristic` piloting the four official starters {iono, dragapult-ex, mega-abomasnow-ex-official, mega-lucario-ex}. N = 50 paired seeds 1..50 per cell. **The `current-v1` row is not rerun: EXP-010's selfdeck arm is cell-identical** (same agent, same deck, same seeds, same iterations, same opponents — stated here at registration time, not decided after looking). So the run is 3 challengers × 4 opponents × 50 = **600 new matches**, ~20 h at EXP-010's ~2 min/game pace. Trajectory recording stays ON (corpus grows; no timing quantity is estimated). **Outcome encoding, read from the producer:** `outcome_for_a ∈ {1, 0, −1}` = win/draw/loss (`local_ladder._sign`) — report W/D/L per cell, not just win rate (lesson from EXP-010's false-draw alarm). Validity: env errors = 0; selfdeck fallbacks reported per cell, investigated before the verdict if > 1 % of decisions. Runner: `bash scripts/run_exp011.sh` (per-cell resume). **Amendment (2026-07-19, before any verdict — after 1 complete cell + 1 in flight):** the first cell (v1-tuned vs iono) closed with **3 TIMEOUT rows** (~603 s each): the engine's 600 s per-seat overage bank killed our seat and scored the games as losses. That bank is a *deployment* constraint — the shipped agent budgets under it (Policy C) and cannot overrun it by construction — while the local arms are iteration-bounded (1000 iters) and never read the clock, so under the local instrument the bank only injects artifact losses against decks whose games run long. Left in place it would tax **only the challengers** (the reused `current-v1` cells closed with 0 timeouts), biasing the test toward the incumbent — the exact direction a default should never be biased. Fix: `--overage-bank` flag in `local_ladder.py` (default 600 preserved — EXP-003..010 semantics untouched; the flag raise changes no decision, search being seeded and iteration-bounded); `run_exp011.sh` now passes 100000; the affected cell is deleted and rerun; the in-flight cell resumes only if it contains no `env_error` rows. **Amendment 2 (2026-07-20, before any verdict — after ~12 h of the second attempt):** the 2026-07-19 fix was **ineffective** — 11 more TIMEOUT rows at ~600–615 s despite `--overage-bank 100000` (v1-tuned: iono seeds 5, 14; dragapult seed 21; abomasnow seeds 12, 42, 43; lucario seed 17; aggro-fire vs iono seeds 4, 5, 6, 10), same artifact, same anti-challenger direction. Root cause read from the engine, not guessed: `env.run()` always calls `reset()` on a fresh env, and the reset rebuilds each seat's state from per-position schema caches (`__state_schema_0/1`) built at `make()` time with the spec's 600 s default — so patching `env.state`/`env.specification` was silently discarded before the first move. Fix: `local_ladder._raise_overage_bank` patches the live spec AND drops those caches, forcing the reset to regenerate from the patched value; regression test `tests/test_local_ladder.py::test_overage_bank_patch_survives_env_reset` pins the failure mode (the patch must survive a reset). Instrument-validation lesson: the 2026-07-19 probe (seed 1) could not have detected the failure — seed 1 never timed out under the old bank; the probe must be a known-TIMEOUT seed (now: v1-tuned vs iono seed 5). The new probe paid off immediately: it caught a schema-corruption bug in the first version of this very fix (the in-memory spec entry is a property-spec *object*; writing a bare number over it fails metaschema validation once the caches are dropped — the bank belongs under its `default` key), before any heal ran. Recovery keeps every clean row: the arms are iteration-bounded and seeded, so the bank cannot change any decision in a game that completed — only the 11 TIMEOUT seeds are deleted and rerun (`scripts/heal_exp011_env_errors.sh`), which yields cells identical to a full rerun at a fraction of the cost. **Amendment 3 (2026-07-22, before any verdict — emboar-evolution row in flight):** a third engine limit surfaced: `runTimeout`, the 2000 s wall-clock cap on the whole episode (`core.Environment.run` compares `perf_counter()` elapsed against `env.configuration.runTimeout` and raises `DeadlineExceeded`). Grindy emboar-evolution matches crossed it twice (vs iono, seeds 7 and 16), and unlike the bank this path *aborts the sweep without writing a row* — no data poisoned, but the run dies. Same argument as the bank: a deployment constraint that cannot change a decision of an iteration-bounded seeded arm, only kill long games by accident. Fix: `local_ladder._make_cabt_env` passes `runTimeout = 2·bank + 2000` at `make()` time whenever the bank is raised (`runTimeout` is honored from `env.configuration` directly — none of Amendment 2's schema-cache surgery applies); regression tests `test_raised_bank_also_unbinds_run_timeout` and `test_default_bank_keeps_ladder_run_timeout` pin both sides. Probe note: this failure is wall-clock-dependent, so no input exercises it deterministically — the crashing seed 7 completed in 120 s on a fresh rerun (vs 2001.9 s at the crash; host suspend inflating the clock is the leading hypothesis, genuine game length the other). Validation is therefore the config assertion in the regression test plus the resumed run itself; completed-game outcomes are clock-independent either way, so no heal is needed — the sweep resumes at the first missing seed. | **Selection rule (pre-registered):** a challenger replaces `current-v1` iff it beats it on the pooled paired contrast across the field (shared seeds and opponents, McNemar exact, **p < 0.05 with Bonferroni m = 3** challengers) AND is not worse than `current-v1` in any single opponent cell by Wilson-separated margin. **The incumbent wins ties by default** — a deck switch this close to #29 must be paid for by evidence, not noise. Report per-cell Wilson 95 % (raw + Bonferroni m = 4) and pooled per candidate. **Branches:** (a) `current-v1` survives ⇒ ADR-002 reaffirmed on real-field evidence, the erratum's re-evaluation debt is paid, #28/#29 proceed unchanged; (b) exactly one challenger separates ⇒ ADR-002 revised, #29 ships that deck; (c) more than one separates ⇒ ship the one with the higher pooled rate (tie-break pre-committed here). Prior: (a), moderately confident — EXP-007's margins were wide (`current-v1` ≥ 66/100 against every challenger) and there is no obvious reason the mirror-informed bias favored the incumbent specifically. | _pending — registered 2026-07-19, before any match ran._ |
